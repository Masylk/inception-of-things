apiVersion: v1
kind: Service
metadata:
  name: app2
spec:
  selector:
    app: app2
  ports:
  - port: 80       # Service port
    targetPort: 80 # Container port
  type: ClusterIP   # Can be NodePort if you want direct access without Ingress
